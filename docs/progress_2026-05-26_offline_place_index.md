# 2026-05-26 離線地點索引

## 本次完成

| 項目 | 狀態 | 說明 | 為什麼重要 |
|---|---|---|---|
| 離線索引資料 | 完成 | 新增 `web_pwa/data/offline_places.json`，目前 53 筆 | 搜尋資料可獨立維護，不必每次改主程式 |
| 類型分類 | 完成 | 加入山名、步道入口、山屋、停車點等類型 | 使用者搜尋時可更快判斷結果用途 |
| App 載入索引 | 完成 | App 啟動時讀取 JSON，失敗時使用內建備援 | 網路或快取異常時仍可保留基本搜尋 |
| 離線快取 | 完成 | Service Worker 預快取 `./data/offline_places.json` | PWA 離線後仍可搜尋索引 |
| Firebase 發布 | 完成 | 已部署到 `https://my-tools-8ac95.web.app` | 手機可直接使用新版搜尋 |

## 驗證結果

| 檢查 | 結果 |
|---|---|
| JSON 解析 | 通過 |
| `node --check web_pwa/app.js` | 通過 |
| `node --check web_pwa/sw.js` | 通過 |
| `npm run smoke` | 通過 |
| Firebase 首頁 | 200，包含 `app.js?v=34` |
| Firebase 索引 | 200，JSON 解析 53 筆 |
| Firebase Service Worker | 200，包含 `hiking-pwa-v34` 與 `./data/offline_places.json` |

## 目前索引類型

- 山名：七星山、合歡山、雪山、玉山、嘉明湖、北大武山等
- 步道入口：小油坑、冷水坑、雪山登山口、塔塔加、向陽登山口等
- 山屋：松雪樓、成功山屋、七卡山莊、三六九山莊、排雲山莊、向陽山屋、嘉明湖山屋、檜谷山莊等
- 停車點：二子坪、武嶺、武陵農場、塔塔加、向陽、北大武等

## 下一步

1. 擴充水源點與山屋資料。
2. 依分區拆成更大的 POI 索引，避免單一檔案過長。
3. 搜尋結果加入多筆候選清單，而不是只跳第一筆。
