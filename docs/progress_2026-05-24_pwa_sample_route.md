# 2026-05-24 PWA 可試用性更新

## 本次完成

| 項目 | 狀態 | 說明 | 為什麼重要 |
|---|---|---|---|
| PWA 首頁文字修復 | 完成 | 重建 `web_pwa/index.html` 的繁中介面文字與語意標籤 | 避免展示時出現亂碼，提升試用信任感 |
| 一鍵範例路線 | 完成 | 新增「範例路線」按鈕，直接載入 `sample/demo-route.gpx` | 減少首次測試摩擦，不必先準備 GPX 檔 |
| 範例 GPX 修復 | 完成 | 修正航點名稱：登山口、稜線休息點、山頂 | 讓路線畫面與航點清單可被正常判讀 |
| 離線快取更新 | 完成 | Service Worker 升版至 `hiking-pwa-v12`，並預快取範例 GPX | 確保安裝後仍可用範例路線驗證離線流程 |

## 驗證結果

| 檢查 | 結果 |
|---|---|
| `node --check web_pwa/app.js` | 通過 |
| `node --check web_pwa/sw.js` | 通過 |
| `node --check web_pwa/server.js` | 通過 |
| `http://localhost:4173/` | 200，含繁中首頁文字 |
| `http://localhost:4173/sample/demo-route.gpx` | 200，含範例路線 |
| `http://localhost:4173/sw.js` | 200，含 `hiking-pwa-v12` |

## 注意事項

- Codex 內建瀏覽器本次拒絕開啟本機網址，因此尚未完成實際瀏覽器點擊截圖驗證。
- 已完成伺服器層級與語法檢查；下一步建議用手機或一般瀏覽器開啟本機/Pages URL，點「範例路線」確認畫布與航點列表。

## 下一步

1. 在可開啟本機網址的瀏覽器中驗證「範例路線」按鈕。
2. 用手機 HTTPS 版本測試安裝、離線快取、GPS 權限。
3. 若要進一步降低測試摩擦，可加上「Demo 模式」自動載入範例路線。
